# Home hero video

Place the main hero video here as `home-hero.mp4` and, when available, an optimized `home-hero.webm` version. The home page uses the Figma hero frame as its poster while the video loads or when no video file is present.
